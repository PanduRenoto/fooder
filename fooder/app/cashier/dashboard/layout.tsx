import CashierTemplate from "@/components/cashierTemplate";
import CashierMenuList from "@/app/cashierMenuList";

export const metadata = {
    title: 'Dashboard | Ordering System',
    description: 'Generated by create next app'
}

type PropsLayout = {
    children: React.ReactNode
}

const RootLayout = ({children}: PropsLayout) => {
    return (
        <CashierTemplate title="Dashboard" id="dashboard" menuList={CashierMenuList}>
            {children}
        </CashierTemplate>
    )
}

export default RootLayout;
